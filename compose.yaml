services:
  tokka:
    container_name: tokka
    build:
      context: .
      dockerfile: build/Dockerfile
      platforms:
        - linux/amd64
    ports:
      - '8090:8090'
      - '8095:8095'
    volumes:
      - ./tokka.yaml:/app/tokka.yaml
    environment:
      TOKKA_CONFIG: ./tokka.yaml
